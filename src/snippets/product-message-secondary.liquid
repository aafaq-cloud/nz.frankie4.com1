{% assign messages = shop.metafields.product_message2 %}

{% for message in messages %}

  {% assign tag = message | first %}

  {% if tags contains tag %}
    <div class="product-message-secondary color-secondary-text">
      {{ message | last }}
    </div>
  {% endif %}

{% endfor %}